<div class="row">
  {% for member in data.members %}
  <div class="col-sm-6 col-lg-3">
    <div class="card mb-4">
      <img src="img/members/{{ member.image }}" class="card-img-top img-fluid" alt="...">
      <div class="card-body">
        <h5 class="card-title">{{ member.name }}</h5>
        <p class="card-text">Click the button below to learn more about this Queen member</p>
        <a href="{{member.slug}}.html" class="btn btn-primary">{{member.name}}</a>
      </div>
    </div>
  </div>
  {% endfor %}
</div>